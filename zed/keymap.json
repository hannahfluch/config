[
  {
    "context": "Editor && !menu",
    "bindings": {
      "ctrl-shift-r": "editor::Format"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "ctrl-t": "workspace::ToggleLeftDock"
    }
  }
]
